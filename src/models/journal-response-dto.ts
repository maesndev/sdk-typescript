/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type JournalResponseDto = {
  id: string;
  accountId: string;
  code: string;
  createdDate: string;
  currency: string;
  description: string;
  type: string;
  updatedDate: string;
};

/** @internal */
export const JournalResponseDto$inboundSchema: z.ZodMiniType<
  JournalResponseDto,
  unknown
> = z.object({
  id: types.string(),
  accountId: types.string(),
  code: types.string(),
  createdDate: types.string(),
  currency: types.string(),
  description: types.string(),
  type: types.string(),
  updatedDate: types.string(),
});

export function journalResponseDtoFromJSON(
  jsonString: string,
): SafeParseResult<JournalResponseDto, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => JournalResponseDto$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'JournalResponseDto' from JSON`,
  );
}
