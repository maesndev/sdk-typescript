/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type TaxRate = {
  id: string;
  code: string;
  name: string;
  taxRatePercentage: string;
};

/** @internal */
export const TaxRate$inboundSchema: z.ZodMiniType<TaxRate, unknown> = z.object({
  id: types.string(),
  code: types.string(),
  name: types.string(),
  taxRatePercentage: types.string(),
});
/** @internal */
export type TaxRate$Outbound = {
  id: string;
  code: string;
  name: string;
  taxRatePercentage: string;
};

/** @internal */
export const TaxRate$outboundSchema: z.ZodMiniType<TaxRate$Outbound, TaxRate> =
  z.object({
    id: z.string(),
    code: z.string(),
    name: z.string(),
    taxRatePercentage: z.string(),
  });

export function taxRateToJSON(taxRate: TaxRate): string {
  return JSON.stringify(TaxRate$outboundSchema.parse(taxRate));
}
export function taxRateFromJSON(
  jsonString: string,
): SafeParseResult<TaxRate, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TaxRate$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TaxRate' from JSON`,
  );
}
